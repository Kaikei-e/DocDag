---
title: A report does not state the seed the run used
kind: clause
modality: MUST_NOT
status: accepted
about:
  - topic/seed-recording
counterexample:
  - pm-0001
date: 2026-01-10
---

# A report does not state the seed the run used

The other half: written when seeds were considered a leak of the harness's
internals. Two strict rules cannot both hold, so one of them has to go — and
that is a decision for the standard's authors, not for DocDag.
