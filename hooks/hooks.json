{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/scripts/docdag-validate.sh",
            "timeout": 30,
            "statusMessage": "Validating the decision graph"
          }
        ]
      }
    ]
  }
}
